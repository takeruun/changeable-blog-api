type BlogList {
  id: ID!
  title: String!
  thumbnailImagePath: String!
  tags: [String!]!
}

type BlogListConnection {
  pageInfo: PageInfo!
  nodes: [BlogList!]!
}

extend type Query {
  blogList(input: PageCondition!): BlogListConnection!
}
