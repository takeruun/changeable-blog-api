input SignUp {
  name: String!
  email: String!
  password: String!
  postalCode: String!
}

type User {
  id: String!
  name: String!
  postalCode: String!
  createdAt: String!
}

extend type Mutation {
  signUp(input: SignUp!): User!
}
